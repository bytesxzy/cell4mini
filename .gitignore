# CELL4 runtime state and data live on the training servers, not in this repo.
# Only code, harnesses and reports are versioned here.
cell4/upstream/rsi/data/
cell4/upstream/rsi/state/
cell4/upstream/rsi/versions/
cell4/upstream/rsi/www/
cell4/upstream/live.json
cell4/upstream/cron.log

# Bootstrap artifact: running cell4.lua from the repo root makes _write_if_missing
# create a genome here. It is runtime state, not source.
/rsi/
